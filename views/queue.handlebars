<ol class="queue-list">
  <li>
    <article class="queue">
      <div class="title">
        <a href="{{article_url}}" target="_blank">{{title}}</a>
        <span>({{article_url}})</span>
      </div>
    </article>
  </li>
</ol>